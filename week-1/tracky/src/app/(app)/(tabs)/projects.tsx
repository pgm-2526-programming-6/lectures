import EmptyView from "@design/View/EmptyView";

const ProjectsLayout = () => {
  return <EmptyView title="No Projects" description="You have not added any projects yet." icon="folder" />;
};

export default ProjectsLayout;
